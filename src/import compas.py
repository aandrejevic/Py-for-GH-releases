# import Rhino
# import scriptcontext
# import rhinoscriptsyntax as rs
# import compas_rhino
import compas

# from compas.compas.datastructures import Mesh

print(compas.__version__)
# mesh = Mesh()

# a = mesh.add_vertex()
# b = mesh.add_vertex(x=1.0)
# c = mesh.add_vertex(x=1.0, y=1.0)
# d = mesh.add_vertex(y=1.0)

